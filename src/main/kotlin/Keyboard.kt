class Keyboard {
    fun readInput(): Int {
        // Placeholder for actual keyboard input. Here we just simulate input.
        // In practice, this would read from the actual keyboard or input source.
        return readlnOrNull()?.toIntOrNull() ?: 0
    }
}
